#include "Board.h"
#include <iostream>
#include <vector>
#include <string>

using namespace std;

/*
Using a std::vector<std::string> variable, 
create a representation of an empty game board with only 
the numbers 1-3, pipes, and dashes, but no x's or o's. 
Each row will be one of the std::strings stored in the vector. 
Data Types and Classes useful here to review strings.
*/

Board::Board() //print the empty board by default constructor
	://constructor initializer lists
{
	vector<string> board
}

string Board::getSpotLocated(int rowNum, int colNum)
{

}

string Board::setSpotLocated(int rowNum, int colNum)
{

}